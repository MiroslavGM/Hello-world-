# Stacks:

browsing_session = []  # Starting our browser;
browsing_session.append(1)  # Going to links, 1, 2, 3;
browsing_session.append(2)
browsing_session.append(3)
print(browsing_session)  # Our list becomes this [1, 2, 3];
# When we go backwards we pop the previous link we were;
last = browsing_session.pop()
print(last)
print(browsing_session)  # [1, 2];
print("redirect", browsing_session[-1])
# If we don't have any more stacked links the button is disabled.
if not browsing_session:
    print("disable")
