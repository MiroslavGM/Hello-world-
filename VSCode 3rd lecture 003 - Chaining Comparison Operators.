age = 18
if age >= 18 and age < 65:
  print("Eligible!")
else:
  print("Not eligible!")
                                # they work the same way.
if 18 <= age < 65:
  print("Eligible!")
else:
  print("Not eligible!")
